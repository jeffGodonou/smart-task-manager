services:
  - type: web
    runtime: docker
    name: smart-task-manager-backend
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: H2_JDBC_URL
        value: jdbc:h2:mem:taskdb;DB_CLOSE_DELAY=-1
